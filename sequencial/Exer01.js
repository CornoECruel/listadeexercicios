function soma() {
    var a, b, soma;  
    a = Number(window.prompt('informe o primeiro valor a'));    
    b = Number(window.prompt('Informe o segundo valor b'));
    soma = a + b;
    window.alert("O valor da soma é, " + soma);
}
